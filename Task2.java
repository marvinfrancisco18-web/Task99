public class Task2 {
    public static void main(String[] args) {

        // primitive variables
        char letter1 = 'V';
        byte two = 2;
        short one = 1;
        int zero = 0;
        float version = 2.0f;
        boolean value = true;

        // concatenate into one string
        String output = letter1 + "" + two + one + one + zero
                + " w" + zero + "rld "
                + version + " " + value;

        // print output
        System.out.println(output);
    }
}
